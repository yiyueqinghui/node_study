// 增、删、改、查；
// 增：
// INSERT INTO 表名 (字段一,字段二..) VALUES （value1,value2..）;
// 删：
// DELETE FROM 表名 WHERE 条件
// 修改：
// UPDATE 表名 SET 字段名=值 WHERE 条件；
// 查询： 
// SELECT 字段一,字段二.. FROM 表名 WHERE 条件；

// = > < AND OR;
